import React from "react";
const apiUri = process.env.REACT_APP_API_URL || process.env.API_URL || "https://paas-backend1.azurewebsites.net/api";

const Login = () => {
  let loading = false;
  const handleChaange = () => { };
  const handleSubmit = () => { };
  return (
    <div>LOGIN</div>
  );
};

export default Login;
